{
  "name": "block-puzzle",
  "version": "0.1.0",
  "description": "Offline Block Puzzle Game",
  "author": "Matthew Murphy",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "npx -y nodemon --watch src --watch api --watch index.html --watch styles.css --ext ts,html,css --exec \"npm run build\"",
    "dev:web": "vite",
    "dev:api": "vercel dev --listen 3001",
    "start": "concurrently -k -n web,api -c cyan,magenta \"npm:dev:web\" \"npm:dev:api\""
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.88.0",
    "@vercel/analytics": "^1.6.1",
    "@vercel/node": "^5.5.15",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "vite": "^6.4.1",
    "vite-plugin-pwa": "^1.2.0",
    "vitest": "^4.0.16"
  }
}